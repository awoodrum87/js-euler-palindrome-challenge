'use strict'

// This function will be called with 3 arguments set to
// 2, 10, and 1000000 respectively

// psuedo code:
// instansiate 2 empty arrays. One for base1 (binary) and base 2 (base 10)
// create a function that accepts base2 and converts it to binary, store
// those binary numbers and base 10 numbers in the corresponding arrays
// instansiate 2 other empty arrays for palindromic number base 2 and base 10
// create a function that determines if base2 array[i] and base 10 array[i]are palindromic
  // use a to.str() method and then something like: str.split('').reverse().join('')

const doubleBasePalindromeSum = function (/* base1, base2, upperLimit */) {
dd
}

module.exports = {
  doubleBasePalindromeSum
}
